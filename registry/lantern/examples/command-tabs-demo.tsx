import { CommandTabs } from "@/registry/lantern/ui/command-tabs";

export default function CommandTabsDemo() {
  return <CommandTabs command="lanterncn add button" type="dlx" className="w-full max-w-xl" />;
}
